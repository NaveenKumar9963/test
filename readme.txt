git init => To create the git as the local repository
git status => checking the file were on the stage or not when files is in color red it they are not in stage
git add <file_name> OR git add . => file name will upload only one file you choosen the dot symbol will let you to upload all the files in the folder.
AGAIN
git status => the files will be in green color then they are ready to transfer to the local repository
git commit -m "<Message>" => the -m will refer to meassage it will define the what changes you made before every push